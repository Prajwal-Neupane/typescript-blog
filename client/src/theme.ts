import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#11009E",
      light: "#C4B0FF",
    },
    secondary: {
      main: "#00DFA2",
      light: "#537188",
    },
  },
});
